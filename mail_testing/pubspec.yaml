name: mail_testing
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'  # Check your Dart version

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.1.14  # Make sure it's at the correct level

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
